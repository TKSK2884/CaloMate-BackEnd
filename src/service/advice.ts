import { connectPool } from "./db";
import { Request } from "express";
import mysql from "mysql2/promise";

export async function name(req: Request, res: any) {
    try {
    } catch {
        return;
    }
}
